export async function decodeResourceChange(resources, coder) {
    const promises = resources.map(async (r) => {
        r.data = await coder.decodeResource(r.data);
        return r;
    });
    return await Promise.all(promises);
}
//# sourceMappingURL=coder-helpers.js.map